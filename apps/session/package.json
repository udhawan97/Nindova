{
  "name": "@nindova/session",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "vite build",
    "dev": "vite --host 127.0.0.1",
    "typecheck": "tsc --noEmit && tsc --project tsconfig.session.json"
  },
  "devDependencies": {
    "vite": "8.1.5"
  }
}
